[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "shelf"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
shelf = "shelf.cli:main"